/* ------------------------------------------
   Development config
   Configuration to override _base for development needs
--------------------------------------------- */

module.exports = config => { // eslint-disable-line
 return {
   publicPath: '/',
 };
};
