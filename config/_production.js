/* ------------------------------------------
   Production config
   Configuration to override _base for production needs
--------------------------------------------- */

module.exports = config => { // eslint-disable-line
 return {};
};
