/* ------------------------------------------
   Server constants
--------------------------------------------- */
const DEFAULT_LANGUAGE = 'en';
const LANGUAGES = [DEFAULT_LANGUAGE, 'pt', 'fr'];

module.exports = {
  DEFAULT_LANGUAGE,
  LANGUAGES,
};
